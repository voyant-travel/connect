---
"@voyantjs/connect-provider-sdk": minor
---

Add the `quoteStay` / `quotePackage` hosted-worker operations (→ `/stays/quote` / `/packages/quote`) — an optional re-price of an offer against the provider's live price just before lock, alongside the existing cruise/stays/packages operations. Protocol version unchanged.
